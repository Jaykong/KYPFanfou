实现一个接口的步骤
一、实现请求的接口
1.从饭否API找到接口
2.定义好接口的常量字符串
3.构造传递的参数(parimeters)

二、数据库插入的接口
1.看接口返回的数据类型
    1)如果是字典，插入一条数据
    2如果是数组，插入多条数据
2.有关系表的Status->User（对象里包含对象）
    1）插入Status表
    2）插入User表
    3）建立关系
三、查询数据库的方法
1.如果frc配置Tableview的
    1）查询方法CoredataTableViewController的子类下configureFetch方法里
2.如果数组配置tabelview的
    1）查询方法就写在你插入数据的方法的

